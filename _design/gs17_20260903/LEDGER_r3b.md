# GS-17 r3b 원장 (2026-09-04, s43)

s42 가 남긴 두 건(S5 라벨 겹침, G4 임계)을 수리하고 게이트를 12/12 로 닫은 라운드.

## 1. S5 확대 라벨을 그림 밖으로

- 지적(r3 반박) = 라벨 `.zl`("크게 보기 ↗")이 그림 안 오른쪽 아래 absolute 라 p1 std_home 의 "21분 7분" 칸과 p5 std_report 의 "2 / 10" 점수를 덮는다.
- 채택 = 자리 이동(그림 밖 캡션 줄). 기각 = hover·focus 시만 표시 — 손가락 기기에서 확대 단서가 사라진다.
- 원천 = `_design/studio_detail_20260903/build/page.css` `.vis .zl` 규칙. 백업 `page.css.bak.20260904_0055_pre_r3b`.
- 대조군(`s5_label_probe.mjs`, 상자 교차) = 수리 전 1440 5/5 · 390 5/5 → 수리 후 1440 0/5 · 390 0/5.
  상자 교차는 잉크 겹침의 상위 집합이라 0 이면 잉크 겹침도 0.
- 수리 후 자리 = 그림 아래 9px, 높이 19px, 5장 전건 표시 상태(`s5_label_r3b.json`).
- 픽셀 채증 = `shots_r3b_zl/{p1,p5}-shot_{1440,390}.png`. p1 의 "21분 7분" 칸이 가려지지 않은 것을 눈으로 확인.

## 2. G4 판정 축 교체 (임계 → 구조)

- 구 식 `booksPct ≤ 72` 는 도달 불가였다. r3 실측 1440 books.top 12492 / H 16984 = 73.6%.
  72 를 만드는 길은 (a) books 를 264px 위로 = #p5 앞으로 올려 "5부 전건 뒤" 순서를 깨거나
  (b) 뒤 절들을 366px 늘려 H 를 2.2% 불리는 지면 개악 둘뿐. 임계 하나가 배치를 정하는 자리라 완화가 아니라 축을 바꿨다.
- 새 축 = 구조 술어(#books 직전 형제 = #p5 AND 직후 = #diff) + 무회귀(booksPct < 80.9, HEAD 82acc5a 실측) + 절정 범위(47.8 ≤ p4Pct ≤ 65), 1440 과 390 양쪽.
- 판정식·계측 함수 단일 구현 = `g4_rule.mjs`. 게이트와 대조군이 같은 모듈을 import(술어 재구현 금지).
- 결함주입 대조군 `g4_controls.mjs` = 살아 있는 지면에서 #books 를 DOM 으로 옮겨 재판정. **6/6 전건 기대대로**.

  | 폭 | 팔 | 기대 | 실측 | books 자리 | booksPct | p4Pct |
  |---|---|---|---|---|---|---|
  | 1440 | as-is(r3) | PASS | PASS | p5 < books < diff | 73.6 | 50.7 |
  | 1440 | HEAD 재현(#faq 앞) | FAIL | FAIL | format < books < faq | 83.6 | 50.7 |
  | 1440 | r2 재현(#p4 앞) | FAIL | FAIL | p3 < books < p4 | 50.7 | 58.3 |
  | 390 | as-is(r3) | PASS | PASS | p5 < books < diff | 70.6 | 49.3 |
  | 390 | HEAD 재현(#faq 앞) | FAIL | FAIL | format < books < faq | 81.1 | 49.3 |
  | 390 | r2 재현(#p4 앞) | FAIL | FAIL | p3 < books < p4 | 49.3 | 61.1 |

  r2 팔은 백분율 축(50.7 ≤ 72)으로는 통과한다. 새 구조 술어만이 가른다 = 축이 실제로 일을 한다.

## 3. 배경 교대 판정 범위

- r3 의 "연속 3" 은 전부 `.part`(5개 부) 연속이었다. 부는 한 덩어리로 이어지는 본문이라 교대 대상이 아니고, 그 안에서 색을 받는 것은 본론 p4 하나뿐(의도, HEAD 동일).
- `r3_checks.mjs` 를 sec 급 한정으로 고쳤다 → **sec 급 연속 0**.
  problem 먹 · who 투명 · trust 담채 · parts 투명 · books 담채 · diff 투명 · format 담채 · faq 투명 · close 먹.

## 4. 게이트·계측

- `baseline_r3c.json` (최종, 커밋 바이트와 일치) = **12/12 PASS**. 그 전 `baseline_r3b.json` 도 12/12(주석 한 줄 차이).
- measure_v3 3면 × 1440/390 = 6조건 전건 PASS. overflow 0, 44px 미만 0, 좌우 잉크 대칭(1440 216/216 · 390 16~20 동일).
- build_all 트리 해시 `16035d93a134e1d1`, 2회 연속 동일(멱등).

## 5. 편집 범위 양방향 대조

page.css 를 되돌렸다가 되살려 r3b 편집이 어느 면에 닿는지 확인했다.

| 상태 | studio.html | guidebook.html | faq.html | 트리 해시 |
|---|---|---|---|---|
| r3b | 8fbb2c440f155bae | c9697901985e17fa | 43a1d876306da51c | 492d5c1caaa3d1c4 |
| 되돌림 | 0b27af5317e11536 | c9697901985e17fa | 43a1d876306da51c | acbac14da506a761 |
| 되살림 | 8fbb2c440f155bae | c9697901985e17fa | 43a1d876306da51c | 492d5c1caaa3d1c4 |

되살린 판이 바이트까지 되돌아왔고, 가이드북과 FAQ 는 세 상태 모두 동일 = r3b 편집은 스튜디오 한 면에만 닿는다.
(그 뒤 CSS 주석의 가운뎃점 한 개를 걷어내 최종 해시는 16035d93a134e1d1.)

미해결로 남긴 것: 되돌린 판의 트리 해시 acbac14d 가 s42 가 기록한 r3 해시 9646ea58 과 다르다.
추적 대상 11파일은 우리 9파일 + rss.xml + sitemap.xml 뿐이고 00:15 이후 바뀐 html/xml/txt 는 build_all 산출뿐이라,
차이는 재생성 산출물(rss·sitemap) 안에 있는 것으로 좁혀진다. 재현하지 못했으므로 "r3 트리 재현" 이라고 적지 않는다.

## 6. 문자 정책

- studio.html 가운뎃점 0(내가 넣은 CSS 주석 1개를 걷어냈다). faq.html 0.
- guidebook.html 3개는 HEAD 와 같은 수이고 전부 09-03 판 CSS 주석 = 우리 회귀 아님. 별건.

---

# r3d 추가 (같은 세션, critic 판정 뒤)

## 7. critic P1 해소 — 스크립트 차단 시 지면 소실

- 지적(critic 가이드북, 32/45 RELEASE NO) = `.rv` 80개가 `@media (prefers-reduced-motion:no-preference)` 안에서 opacity 0 으로 시작하고, `.in` 부여는 본문 끝 인라인 스크립트뿐이다. 스크립트를 막으면 카탈로그와 가격과 CTA 가 통째로 사라진다.
- 원천 확인 = 그 스크립트에 IntersectionObserver 미지원 폴백은 이미 있다. 남은 구멍은 스크립트 차단 하나뿐.
- 집행 = 두 빌더 head 의 `<style>{{CSS}}</style>` 뒤에 `<noscript><style>.rv{opacity:1;transform:none}</style></noscript>`.
  백업 `build_page.py.bak.20260904_0125_pre_noscript` 두 벌.
- 대조군(`nojs_probe.mjs`, javaScriptEnabled false, 1440) = 라이브 구판을 "고치기 전 세계" 팔로 삼았다.

  | 팔 | 면 | `.rv` 비가시 | 핵심 절 |
  |---|---|---|---|
  | 로컬 새 판 | studio | 0/52 | #format 0/4, #close 0/2, #units 0/7 |
  | 로컬 새 판 | guidebook | 0/80 | #books 0/32, #format 0/2, #close 0/2 |
  | 라이브 구판 | studio | 52/52 | #format 4/4, #close 2/2, #units 7/7 |
  | 라이브 구판 | guidebook | 80/80 | #books 32/32, #format 2/2, #close 2/2 |

  구판 수치가 critic 이 보고한 값과 자리까지 일치한다. 지적은 실재였고 지금 라이브에서 그대로 돌고 있다.
- 범위 = 우리 두 면의 빌더만 고쳤다. `faq.html` 도 `.rv` 6개를 쓰는데 그 정의는 사이트 공용 자산에 있어 별건으로 남긴다.

## 8. 확대 안내 문면 강화 (audience 권고 b)

- 지적 = 그림 밖으로 내리자 자리가 사진 출처 표기와 같아져 "설명" 으로 읽히고 "눌러 보라" 로는 안 읽힌다. 390 에서 그림 속 글자가 안 읽히는 만큼 이 안내가 제일 중요한데 제일 조용했다.
- 집행 = 크기(12px 모노)와 자리는 그대로, 회색에서 먹색 밑줄로. hover 와 focus 에서 밑줄 두께 2px.
- 기각 = 테두리 상자로 키우는 안. 그림과 경쟁한다.

## 9. r3d 재검증 (두 변경 뒤 전종 재실행)

- 게이트 `baseline_r3d.json` = **12/12 PASS**
- G4 결함주입 대조군 = **6/6 전건 기대대로**
- `.zl` 상자 교차 = 1440 0/5, 390 0/5
- measure_v3 3면 × 1440/390 = **6조건 전건 PASS**
- sec 급 배경 교대 = **연속 0**
- build_all 트리 해시 `505298def40faeda`, 2회 연속 동일
- 가운뎃점 = studio 0, guidebook 3(HEAD 와 같은 수, 09-03 판 CSS 주석)

## 10. critic 재채점

| 면 | r1 | r3b | r3d | 판정 |
|---|---|---|---|---|
| 스튜디오 | 35/45 P1 1 | 36/45 P1 0 YES | 재확인 대기 | 렌즈 6 상승분 = 확대 경로 신설 후 라벨 자리 정정 |
| 가이드북 | 32/45 P1 0 | 32/45 P1 1 **NO** | **33/45 P1 0 YES** | 렌즈 8 매체 적합성 3/5 → 4/5 |

가이드북 critic 의 독립 실측(javaScriptEnabled false, 캐시 우회) = 신판 `.rv` 0/80 비가시, 1440 과 390 동일.
JS 켠 상태 회귀 없음(#books 순차 등장 0/32, 급속 스크롤 0/32). 면수 합 1,178 과 질문 합 3,065 종전과 동일.
5점을 주지 않은 이유로 남긴 것 = 390 기준 25,002px 문서에 CTA 가 0.2% 와 95.1% 두 자리뿐이고 결제 바가 없다,
스킵 링크 부재. 둘 다 GS-17 12항목 밖이라 후보로 남긴다.

가이드북 P2 5건은 전건 잔존 = 가격 카드 죽은 하단(54.7%, 60.1% 대 5.9%), mono 한글 공백 2.39배,
H2 에서 H4 건너뜀 5개 절, `#books .mt` 11px 위계 최하단, 390 마지막 행 고아 1개.

**최종 = 스튜디오 37/45 YES P1 0, 가이드북 33/45 YES P1 0.** 두 면 모두 릴리스 조건(31 이상, P1 0) 충족.
스튜디오 상승분은 렌즈 9(접근성) 4 → 5, P2 1번 소멸. 확대 안내 대비 5.04 → 11.73.
朱印 요소 수는 9(1440)와 10(390)으로 불변 = 먹색 밑줄은 액센트 총량에 가산 0 이고 기존 링크 어휘에 흡수됐다.
audience = 스튜디오 25.5/30, 가이드북 25.5/30. 둘 다 문턱 25 통과.

## 11. 배포 (2026-09-04 01:2x)

`sh _tools/deploy.sh` 실행. wrangler 가 6파일 업로드(sitemap.xml, rss.xml, faq.html, programs 2, std_stage_prep.jpg).

**부분 실패 1건** = routes PUT 이 503(`upstream connect error`, Ray a35619b229f2a865). `set -e` 로 스크립트가 거기서 멈춰
edge_smoke 와 IndexNow 는 수동으로 이어 돌렸다. 실측하니 서빙에는 영향이 없다.
`wrangler.toml` 의 두 route 는 이미 같은 값으로 등록돼 있어 갱신 내용이 없었고, 실패해도 기존 route 가 살아 있다.
apex 200, www 301, 새 판 서빙 전건 확인. 프록시 환경변수 경고가 함께 떴으므로 재발 시 `env -u` 4종으로 재시도한다.

**라이브 채증 (hyunhak.com 직접 실측)**

| 항목 | 배포 전 | 배포 후 |
|---|---|---|
| 게이트 12항목 | 해당 없음 | **12/12 PASS** (`baseline_live.json`) |
| `#books` 자리 | format 뒤 faq 앞 | **p5 뒤 diff 앞** |
| 발행일 | 2026-09-03 | **2026-09-04** |
| noscript 폴백 | 없음 | **있음** |
| 스크립트 차단 비가시 | studio 52/52, guidebook 80/80 | **0/52, 0/80** |
| 확대 안내 상자 교차 | 해당 없음 | **1440 0/5, 390 0/5** |
| measure_v3 3면 × 두 폭 | 해당 없음 | **6조건 전건 PASS** |
| edge_smoke | 해당 없음 | **16항목 FAIL 0 WARN 0** |
| IndexNow | 해당 없음 | **200, 53 URL** |
