**릴리스 보류입니다. 접합 슬릿은 해결됐지만, 새 키보드 스크롤 경로가 자동 넘김을 멈추지 않습니다.** 두 커밋과 6차 회신, 현재 코드, 제공 이미지를 검토했습니다. 브라우저 재실행은 하지 않았고 파일 변경도 없습니다.

| (a) 기존 지적 | 판정·근거 |
|---|---|
| 접합 슬릿 | **해결.** [base.css:942](/Users/gregory/Workspace/_wt/hh-studio12/assets/base.css:942)의 −1px 겹침으로 투명 테두리를 덮습니다. 제공 crop 36장을 **전체 행 기준으로 독립 재검사**해 어두운 슬릿 행 0건을 확인했습니다. |
| 아래 맞춤 빈 공간 | **잔존, 수용 가능.** [base.css:891](/Users/gregory/Workspace/_wt/hh-studio12/assets/base.css:891)의 최대 슬롯 높이와 아래 맞춤에서 발생합니다. 조작부 위치 보존을 우선한 판단은 타당합니다. 다만 고정 하단바와 공백 축소가 모든 설계에서 불가능한 관계는 아닙니다. |
| 짧은 화면 하단바 잘림 | **해결.** [app.js:481](/Users/gregory/Workspace/_wt/hh-studio12/assets/app.js:481)의 높이 측정과 [base.css:906](/Users/gregory/Workspace/_wt/hh-studio12/assets/base.css:906)의 높이 상한이 연결됩니다. 제공 7크기 실측과 일치합니다. |
| N≥3 감아 돌기·먼 카드 노출 | **해결.** [app.js:499](/Users/gregory/Workspace/_wt/hh-studio12/assets/app.js:499)의 순간 이동과 `data-far`가 해당 경로를 처리합니다. 제공 교차 표본은 나가는 활성 카드입니다. |
| 첫 초점에서 스크롤 불가 | **스크롤 자체 해결, 자동 넘김과의 충돌 잔존.** 아래 P2 참조. |
| 폭<600·높이≤330에서 둘째 줄 잘림 | **해결.** 같은 높이 제한으로 처리되며 568×320·320×256 실측이 뒷받침합니다. |

(b) 회귀 검토입니다.

- **P2·차단: 키보드로 읽는 중 카드가 자동 교체됩니다.** [app.js:533](/Users/gregory/Workspace/_wt/hh-studio12/assets/app.js:533)은 스크롤만 하고 `setPause(true)`를 호출하지 않습니다. 초점은 `.pdim`에 남으므로 [app.js:577](/Users/gregory/Workspace/_wt/hh-studio12/assets/app.js:577)의 포인터·초점 정지 처리도 작동하지 않습니다. 실제 소스 함수와 타이머 대역을 실행해 **스크롤 0→48, stopped=false, 이후 cur 0→1**을 확인했습니다. 키 스크롤을 처리할 때 자동 넘김을 정지하고, 타이머 경과 후에도 같은 카드인지 검증해야 합니다.
- **−1px 겹침:** [base.css:942](/Users/gregory/Workspace/_wt/hh-studio12/assets/base.css:942)의 불투명 배경·z-index가 카드 그림자를 덮습니다. 활성 카드 아래 모서리는 직각이고, 하단바 아래 둥근 모서리는 유지됩니다. 겹치는 영역은 테두리이며 첫 줄 버튼 높이는 줄지 않습니다. 초점 윤곽은 별도 규칙으로 유지됩니다. 새 차단 결함은 확인되지 않았습니다.
- **높이 폴백:** ResizeObserver 부재가 곧 `0px` 사용을 뜻하지 않습니다. 최초 동기 측정과 resize 대체 처리가 있습니다. 다만 관찰자가 없으면 창 resize 없는 글꼴·내용 높이 변화는 놓칩니다. 관찰자가 있는 경로도 콜백 전에는 이전 측정값을 사용합니다. 비차단 잔여 후보입니다.
- **전이·입력:** reduce 규칙은 유지되며 측면 클릭·스와이프는 같은 `go()`를 사용합니다. [base.css:898](/Users/gregory/Workspace/_wt/hh-studio12/assets/base.css:898)의 pointer-events 차단과 카드 inert로 먼 슬롯 입력도 막습니다. 이중 rAF 복구에는 세대 검사가 없어 초고속 연속 입력은 미검증 후보입니다.
- **Space·스크린리더:** DOM 초점이 정확히 `.pdim`일 때만 스크롤하므로 버튼 Space 활성화는 가로채지 않습니다. 가상 커서는 DOM 초점과 구분해야 합니다. [NVDA 문서](https://download.nvaccess.org/documentation/en/userGuide.html#BrowseMode)의 탐색·포커스 모드 차이상, 코드만으로 충돌을 확정할 수 없으며 실제 낭독은 미검증입니다.

RELEASE: NO