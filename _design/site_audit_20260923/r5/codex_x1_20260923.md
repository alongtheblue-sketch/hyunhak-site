**4차 P1은 해결됐지만 릴리스는 보류합니다.** `507edf1` 코드·4차 회신·r5 원자료·첨부 이미지를 대조했습니다. 브라우저 재실행은 하지 않았으며, 아래 수치는 제공 실측입니다. 파일은 수정하지 않았습니다.

| 항목 | 판정과 근거 |
|---|---|
| `popnav_p1` | **해결.** [app.js:540](/Users/gregory/Workspace/hyunhak-site/assets/app.js:540)의 `.pbar` 제외 조건이 같은 클릭의 버블링에 의한 되돌림을 막습니다. 클릭·탭·Enter·Space·방향키·측면 클릭·스와이프 58/58 통과, 동작당 새 활성 슬롯 1개입니다. |
| `pause_semantics` | **마크업 의미 해결, 터치 동작 미해결.** [app.js:483](/Users/gregory/Workspace/hyunhak-site/assets/app.js:483)이 글자와 접근성 이름을 함께 갱신하고 `aria-pressed`를 제거합니다. 동작명이 바뀌는 [APG 회전 단추 패턴](https://www.w3.org/WAI/ARIA/apg/patterns/carousel/)에 부합합니다. |
| `font_boundary` | **해결.** [base.css:375](/Users/gregory/Workspace/hyunhak-site/assets/base.css:375)의 `nth-child(n+2)`가 1열에서 두 번째 이후 괘선을 모두 복원합니다. 기본 글꼴 32px·폭 1150/1200px 통과, 1201px은 정상 3열입니다. |
| `footer_all` | **해결.** [base.css:455](/Users/gregory/Workspace/hyunhak-site/assets/base.css:455)로 공통화했습니다. compact 7면 포함 77면×2폭에서 정렬 통과, 좌우 여백 차이 최대 0.02px입니다. |
| `empty_border` | **해결.** [base.css:368](/Users/gregory/Workspace/hyunhak-site/assets/base.css:368)의 `border:0;border-radius:0`으로 점선을 제거했습니다. 3폭에서 목록 위아래 실선만 남습니다. |
| `junction` | **해결.** [base.css:900](/Users/gregory/Workspace/hyunhak-site/assets/base.css:900)이 활성 카드 아래 테두리를 제거합니다. 하단바 윗선만 1px이며, 남은 그림자 띠는 기존 현상입니다. |
| `transition` | **시각 동기 해결.** [base.css:898](/Users/gregory/Workspace/hyunhak-site/assets/base.css:898)의 바탕·내용 명도 전이가 슬롯 transform과 같은 500ms/easing을 사용합니다. 실측 진행률 편차 최대 0.013입니다. |

회귀 검토 결과는 다음과 같습니다.

- **측면·스와이프 차단 없음.** `.pbar` 조건은 슬롯의 `click` 처리만 제한합니다. 측면 슬롯 클릭과 별도 `touchstart/touchend` 경로([app.js:544](/Users/gregory/Workspace/hyunhak-site/assets/app.js:544))는 유지됩니다.
- **Label in Name 위반 없음.** 「자동 넘김 정지/재생」은 보이는 「정지/재생」을 포함합니다. 화살표는 기능을 나타내는 기호이므로 「이전/다음 안내」가 적절합니다. [WCAG 2.5.3 해설](https://www.w3.org/WAI/WCAG22/Understanding/label-in-name.html)
- **reduce 대응 정상.** [base.css:901](/Users/gregory/Workspace/hyunhak-site/assets/base.css:901)이 세 전이를 모두 제거하고, [app.js:480](/Users/gregory/Workspace/hyunhak-site/assets/app.js:480)이 정지 상태로 시작합니다. 미지원 환경에서는 일반 전이·자동 넘김이 유지됩니다. 추가된 색·명도 전이는 적용되지만 기존 transform보다 전이 시간이 늘지는 않습니다. 미지원 실기 검증은 없습니다.
- **푸터 특이도 회귀 없음.** 공통 선택자는 `(0,1,2)`이며, 더 강한 페이지 내부 선택자는 현재 글자 크기만 지정합니다. 동일 속성 충돌이 없습니다.

**남은 결함의 심각도:**

- **터치 재개 실패: P1.** [app.js:541](/Users/gregory/Workspace/hyunhak-site/assets/app.js:541)의 호환 `mouseenter`가 `hover=true`를 남겨 504행에서 타이머 재개를 막습니다. 두 시나리오 모두 20초간 넘김 0입니다. 표시는 서로 일치하지만 실제 동작과 어긋납니다. 이번 커밋이 만든 회귀는 아닌 기존 기능 결함입니다.
- **같은 좌표 연타 오작동: P2, 출시 전 수정 필요.** [app.js:492](/Users/gregory/Workspace/hyunhak-site/assets/app.js:492)의 바 재부착과 슬롯 이동으로 다음 클릭이 딤·체크박스에 맞습니다. 56회 중 닫힘 9회, 체크 변경 6회입니다. 체크는 이후 닫을 때 하루 숨김으로 저장될 수 있습니다.
- **카드 높이에 따른 표적 이동: P2.** 공지 추가 시 최대 166.3px 이동하여 300~700ms 간격에서도 닫힙니다. 현재 공지 0건이라는 조건으로 결함이 해소되지는 않습니다.

터치 재개와 하단바 표적 안정성을 수리한 뒤 재검토해야 합니다.

RELEASE: NO