# astra 전면 검열 수정 2차 실측 (2026-09-23 08:2x, 로컬 8092 정적 + 8799 모킹 API)

측정 = Playwright MCP getBoundingClientRect / getComputedStyle. 스크린샷 = `shoot_r2.mjs` (system Chrome, `.rv` 강제 표시, 1440×900 과 390×844, fullPage). 결과 = `shots_r2/<면>_<폭>.png`. 40면 전부 scrollWidth = 뷰포트 폭 (가로 넘침 0).

## 1. 팝업 무대 (critic 1차 32/45 NO → P1 수리)

| 항목 | 1440 | 390 |
|---|---|---|
| 측면 슬롯 opacity | 1 (종전 .55) | 0 (숨김, 종전과 같음) |
| 측면 카드 바탕 | rgb(235,228,212) = --mat 불투명 | 해당 없음 |
| 측면 내용 .pfr opacity | .55 | 해당 없음 |
| 활성 카드 cx / 폭 | 720 / 440 | 185 / 357 (뷰포트 중심 195, 패딩 안 중심) |
| 하단바 | 폭 440, 활성 카드 아래 | 2줄, 이전/다음/정지 cx 185 = 카드 cx |
| 정지 단추 | 48×48. 클릭 → 「재생」 aria-pressed=true 로 멈춤 (종전 pointerdown 뒤 click 이 재생시키던 역전 해소) | 동일 코드 |
| 자동 넘김 | 8000ms (종전 5000) | 동일 |
| 제목 초점 outline | none (h2 만, 조작 요소 :focus-visible 유지) | 동일 |
| .pacts 열 | 229 : 153 (3fr 2fr, 종전 50:50) | 1열 |
| 체크박스 | 18px calc 토큰, 체크 글리프 있음 | 동일 |
| scrollbar-gutter | html.ppop-open stable | 동일 |
| .pop role | group (의뢰, 행사, 공지 3종) | 동일 |
| ESC 뒤 | 무대 제거, inert 0, overflow visible | 동일 |

## 2. 홈 대학 목록 (critic 1차 33/45 YES 조건부)

| 항목 | 1440 | 390 |
|---|---|---|
| .find .search 폭 | 448 (min(100%,28em), 종전 240) | 350 = 본문 폭 (종전 180) |
| 검색 단추 | 48×48 (종전 44) | 48×48 |
| 행 괘선 | 타일 x 216/552/888 폭 336 맞닿음(gap 0, 우측 패딩 40), 첫 행 border-top 0, 이후 1px, 목록 border-bottom 1px | 1열, 첫 행 0, 이후 1px |
| a11y | input aria-describedby 해제, .tiles aria-live 해제 + aria-describedby=q2h, 낭독 = #q2n 한 줄 400ms 디바운스 | 동일 |
| 죽은 코드 | 탭 변수, 핸들러, f==='sale' 분기 제거, 빈 상태 문구에 목록 링크, 타일 title | 동일 |
| 안내 순서 | #products 153 → .abnd 1011 → #why 1203 → #find 1381 | 100 → 1593 → 1859 → 2151 |

## 3. 터치 타깃 48 (astra A P0-3, B P0-2, C P0-3, D P1-11)

헤더 검색 단추 48, 홈 카드 「자세히」 48×48, 위치 링크(.crumb a) 48, 행사 띠 .promo .in/a 48 (--promo-h = --tap + --line), 칩(.chips button) 48 (인강 #crsChips, 랭킹 setnav, 스튜디오 난이도), 푸터 링크 48×48, 동의 라벨 48 (의뢰 2행, 고객센터, 스쿨, 가입 5행 .check-choice, 약관 전문 .view 48×48).

## 4. 면별 (1440 / 390)

| 면 | 확인 |
|---|---|
| 의뢰 | 390 가격표 폭 350 = 래퍼, 금액 열 x 237~370 화면 안, table-layout fixed, min-width 0 (종전 520 가로 스크롤) |
| 가이드북 목록 | 탭 0, 표지 img 0, 행 65px 괘선 2열(1440) / 1열(390), 대학명 좌측 + 캠퍼스 small 한 줄, 검색 폭 448(1440) / 307(390), 요약 할인가 글자 = 정가 글자 크기, #allpass 는 목록 뒤 |
| 가이드북 소개 | .sample.pgs 가 .r2-samples 밖(뒤), 갤러리 align start, 캡션 간격 16, #preview 확대 링크 4, #books 390 = 1열 괘선 행 48 (표지 숨김) |
| 스튜디오 소개 | #preview 확대 링크 4, 카드 발 = tlink / btn ghost sm / btn sm, 390 카드 좌우 패딩 16 |
| 스튜디오 | 「응시 단위 12곳」 = 카드 12 (renderUnits 가 수 갱신), 안내 .abnd 가 phead 뒤, 390 카드 패딩 16 + 발 wrap |
| 인강 | .cr 2열 680:288(1440) / 1열(390), .sub 숨김, OT svg 숨김, aeo 1문장(51자), 순서 번호, 배지 회색 |
| 판정표 | phead 167 → .abnd 880 → #jt 1648, .badge.doc 한지 바탕 먹색 51개, 朱印 배지 0, #jt 검색 48 |
| 전형 상세 | .xtype 128/252/252/303 (마지막 열 확대), 본문 16px, 朱印 = 가격 .sale 만 |
| 연구소 | .maker.a 1열 928, .mk-side 숨김, 경력 16px/28px, 글리프 곡률 토큰 |
| FAQ | 목차 y 483 < 질문 903 (390), 2열 142, 링크 48, 정가 상자 제거 |
| 공지 | notice-head wrap max 688 = 본문 열, 제목과 목록 같은 열 |
| 고객센터 | 동의 행 label 48, 순서 textarea 995 → 안내 1202 → 동의 1511, 환불 p 16px, 약관 링크 #art6 |
| 스쿨 플랜 | panel.school-inquiry 지면 바탕 먹색 글자, 동의 label 48, 상태 글자 --body, 기능 h3 = p = 16px, 이메일 tlink |
| 가입 | 5행 .check-choice 57~127 높이, .view 48×48, rail 숨김, step 괘선(bg 투명, radius 0), mtype 4px 13px |
| 로그인 | .entry 패딩 0 괘선 0, 버튼 440 = 입력칸 440 |
| 약관 | .legal-nav 2링크 48 (#age, #art6), .updated 제목 아래, 본문 576 = 목록 576 |
| 자료실 | body.library-page, 가격 정가 33,000 취소선 + 할인가 23,100 (같은 글자 크기), .lay 648+320 sticky, #lecdocs padding-block |
| 랭킹실 | .setnav 661 < 표 725, 칩 48 |
| 기출 체험판 | 리드 교체, 안내 노트 하단, aeo 1문장, 중복 조건 3건 제거 |

## 5. 적용 보류 (사유)

- astra A P0-5 상세면 행사 띠 초기 hidden: /api/config 전 0.2~0.8초 가격 미적용 플래시를 없애려면 띠가 응답 뒤 나타나야 해 매 로드 44px CLS 가 생긴다. app.js 는 이미 판정 불일치, 만료, 미확정이면 띠를 숨긴다. 행사 종료 9/30. 보류.
- astra A P1-6 데스크톱 스튜디오 카드 padding s4 전체: 홀수 열 좌 0 = 페이지 스파인 규범(Stage 3 + critic Δ2-R1, 2026-09-10) 과 충돌. 390 만 적용.
- astra D P1-9 인강 .aeo-answer 삭제: seo_inject 가 manifest answer 로 매 빌드 재삽입(speakable). 삭제 대신 manifest 문장을 lede 와 겹치지 않는 1문장으로 교체.
- 인강 상세면(lecd) 맛보기 배지, 앵커 朱印, exam .xc 타임라인 점 1개: 이번 범위 밖(에이전트 보고). 다음 회차.
