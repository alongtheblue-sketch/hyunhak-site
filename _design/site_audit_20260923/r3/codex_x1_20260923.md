## (a)

- **해결 — motion.** [app.js:482](/Users/gregory/Workspace/hyunhak-site/assets/app.js:482)가 감소 모드의 초기 단추를 「재생」·`aria-pressed=true`로 맞춘다. 495행의 `setPause()`, 536행의 클릭 처리, 558행의 타이머 시작 조건도 일치한다. 요청한 초기 상태와 토글 기능은 해결됐다. 접근성 의미 문제는 아래에 별도로 남긴다.
- **부분 해결 — tiles.** [base.css:366](/Users/gregory/Workspace/hyunhak-site/assets/base.css:366)의 열별 리셋으로 1024·768px의 세 번째 타일 괘선 누락과 열 사이 간격 문제가 해결됐다. 그러나 421~600px 충돌은 남았고, 소수 폭 공백은 새로 생겼다.
- **해결 — join.** [join.html:59](/Users/gregory/Workspace/hyunhak-site/join.html:59)의 `nowrap`과 다음 행의 `span{flex:1;min-width:0}`가 전역 `flex-wrap:wrap`을 이긴다. 체크박스는 축소되지 않고 설명만 줄바꿈된다. 「전문」 링크도 라벨 밖 형제로 유지된다.
- **해결 — books.** [guidebook.html:166](/Users/gregory/Workspace/hyunhak-site/programs/guidebook.html:166)과 [템플릿:92](/Users/gregory/Workspace/hyunhak-site/_tools/program_guidebook_v2.html:92)가 모두 `return_buy`로 변경됐다. 문구는 「구매할 상품 고르기」이며, 대상 `#buy`도 102행에 존재한다.

## (b)

- **421~600px: 기존 결함, 릴리스 차단.** `c5dff51`부터 존재한다. [base.css:374](/Users/gregory/Workspace/hyunhak-site/assets/base.css:374)의 1열 지정은 뒤의 465·487행에 덮이지만, 1열용 패딩·괘선 리셋은 남는다. 따라서 실제 2열에서 열 사이 간격이 사라지고 첫 행 오른쪽에 이중선이 생긴다. `tiles_600.png`에서도 직접 확인했다. 라이브 `44aea38`의 상자형 목록에는 없는 악화다.
- **1100~1101px: 신규 회귀.** 366·367행의 미디어 조건이 모두 거짓이어서 기본 3열의 첫 행 괘선과 마지막 열 패딩이 초기화되지 않는다. 수정 전에는 무조건 적용하던 규칙이다. 글자 충돌이나 조작 장애는 없어 **단독 차단 사유는 아니지만** 경계 수정에 포함해야 한다. 600~600.016px에도 이론상 조건 공백이 있다.
- **다른 면: 신규 회귀 미발견, 비차단.** 배포 HTML에서 `.tiles`는 홈만 사용한다. 가입 수정은 `#joinForm`에 한정돼 request·support·b2b·my의 `.check`에 전파되지 않는다. 공통 팝업은 다른 페이지의 API 공지에도 적용되지만, [app.js:471](/Users/gregory/Workspace/hyunhak-site/assets/app.js:471)의 `N>1` 조건과 단추 존재 검사가 단일 카드 경우를 보호한다.
- **검색 필터: 신규 회귀 없음, 비차단.** [index.html:231](/Users/gregory/Workspace/hyunhak-site/index.html:231)은 필터 결과로 자식을 다시 생성한다. 숨겨진 요소가 `nth-child` 순번에 남지 않는다. 불완전한 마지막 행의 빈 칸 괘선과 0건 안내 폭은 기존 구조다.
- **접근성 의미: 기존 결함, 이번 수리의 신규 회귀는 아님.** [app.js:497](/Users/gregory/Workspace/hyunhak-site/assets/app.js:497)은 정지 상태를 「재생, 눌림」으로 노출해 재생 활성 상태처럼 해석될 여지가 있다. 실제 이중 낭독은 스크린리더 실측 없이 단정할 수 없다. [W3C 캐러셀 패턴](https://www.w3.org/WAI/ARIA/apg/patterns/carousel/)에 따라 동작 라벨을 바꾸려면 `aria-pressed`를 제거하는 편이 적절하다. **단독 차단으로 보지는 않는다.** 호버 중 정지는 기존 동작이며 비차단이다.

## (c)

`87106d8`은 세 건을 해결했지만, 라이브보다 대학 목록의 구분이 나빠지는 421~600px 결함을 남긴다. 열 수와 괘선·패딩의 적용 경계를 일치시키기 전에는 릴리스를 보류한다. 판정은 코드·커밋·제공 이미지 대조에 근거하며 브라우저 프로브를 재실행하지는 않았다.

RELEASE: NO