# design-critic(opus) 6회차 채점 회신 (2026-09-23 15:0x 발주 wf_372e1171-02e, 15:2x 회신, 대상 3d2ada3. Codex X1 병렬 = r6/codex_x1_20260923.md, RELEASE: NO)

요약: 팝업 무대 35/45 릴리스 YES, 대학 목록 37/45 유지, 릴리스 전 P1 0건. 반증 2렌즈 차단 0건. 프로브: 행사 카드 활성 1440 접합 겹선(투명 아래 선 회귀), regress: 카드와 하단바 사이 1 CSS px 슬릿 5/18 상태(수리 전 0/18, 하단바가 슬롯 레이어 밖으로 나가며 좌표 반올림이 갈림).

usecase_id: 반응형 웹 상품 판매 지면의 첫 진입 안내 팝업(화면 px, 1440/1024/768/430/390). 지배차원은 그리드(조작부 위치)다.
Stage 1: 서빙 파일 3개는 3d2ada3 과 같다. 팝업이 없는 면 40쌍은 AE 0 이거나 같은 판 재촬영에서 재현되는 (b)다. 짧은 창 10/10 에서 하단바가 창 안에 있다. PASS. 추적 파일 수정은 0이다.

## §1 팝업 무대: 35/45, 릴리스 YES
L1 4 / L2 4 / L3 4 / L4 4 / L5 4 / L6 4 / L7 3 / L8 4 / L9 4

- **L6 3→4.** 넘김 첫 프레임의 세로 튐이 없어졌다. critic 직접 실측: 나가는 카드 top 이 1440 에서 212.7→212.7→215.1(0/17/33ms), 390 에서 163.6→166.2→169.2 로 움직였고, 무대 높이는 503.1/613.8px 로 고정이다. 5차에는 +28.5/+53px 튀었다. 150ms 프레임에서는 들어오는 카드가 하단바보다 68px 오른쪽에 있어, 약 300ms 동안 카드와 하단바가 떨어져 보인다. 감점하지 않았다.
- **L8 3→4.** 하단바 부모가 .pstage 인 경로 96/96, 넘기는 동안 이동 0px(41프레임). 연타 스윕 64/64 에서 닫힘 0, 체크 0(5차는 56회 중 9회와 6회). → 위치 편차 0(5차 141.3/166.3px).
- **L9 3→4.** 첫 초점은 대화상자 「안내 2건」이다. 20초 동안 2회 넘어가는 사이 focusin 은 1회뿐이다. Tab 순환 7개(inert 측면 제외), ESC 초점 복귀 4/4, 390 탭 재개 8031ms. 남은 것: 스크린리더 읽기 모드에서는 8초 뒤 읽던 카드가 inert 가 된다. 정지 버튼은 탭 순서 6번째다.
- **L7 4→3.** 3d2ada3 회귀다. 카드와 하단바 사이 1 CSS px 행에 딤과 지면이 비친다(98~156 회색). critic 이 5/18 상태를 재현했고, dsf 1 과 2 에서 같다(홈 → 뒤 1440과 1280, 홈 첫 표시 1024, 입구 면 N=1 1440과 1280). 입구 5면에서는 첫 화면에 1x 로 보인다.
- **L4 4 유지. 아래 맞춤은 허용한다.** 짧은 공지가 활성이면 카드 위 딤 공간이 282.4px(1440), 332.5px(390, 카드 top 53%)이다. 조작부를 고정해 연타 오작동을 없앤 대가로 본다. 남은 것: N=3 1440 에서 측면 카드 아랫선이 셋(643.6, 621.3, 635.4)으로 갈린다. scale(.9) 의 기준점이 가운데라서 생긴다.
- 셀프 점수가 제출되지 않아 Δ 는 없다.

Codex X1 병렬 채점 = 같은 워크플로의 별도 Codex 에이전트, 메인 세션이 병합

## §2 대학 목록: 37/45, 릴리스 YES (유지)
CSS 변경은 891~941행 팝업 블록 안에만 있다. index_tiles 1440/390 AE 0, 격자 6폭 identical, box_dy 0.

## §3 수리 확인
| 항목 | 판정 | 근거 |
|---|---|---|
| 5차 P2-1 터치 재개 | 해소 | 390 탭 8031ms, 20초에 2회 |
| 5차 P2-2 하단바 이동, 세로 튐 | 해소 | §1 L6, L8 |
| 5차 P2-3 hover 잔류 | 해소 | base.css:947 |
| 5차 P2-4 그림자 띠 | 해소 | 접합 아래 그림자 행 0 |
| 반증 P2-1 초점 이동 | 해소 | 20초 뒤 activeElement = .pdim |
| 반증 P2-2 390 재생 | 해소 | P2-1 과 같음 |
| Codex 5차 P1, P2 2건 | 해소 | 위 행들 |
| 접합 겹선(5차 해소) | 재발 | §1 L7 |
| 5차 이월 5건 | 미착수 | 범위 밖 |

## §4 릴리스 전 P1
없음. 슬릿은 1px 짜리 외관 결함이고 조작과 접근성에는 영향이 없다. 라이브 94f577b 에 남아 있는 연타 오작동(체크 6/56)보다 가볍다.

## §5 P2
1. **접합 슬릿.** base.css:941 .pbar 에 `margin-top:calc(-1 * var(--line))` 를 더한다. 주입 실측 0/36(18상태 x dsf 1, 2), 대조군 5/18. 900행 `border-bottom-color:var(--card)` 를 함께 넣어도 0/36 이다. 같은 배포에 넣는다면 popnav 의 nav, sweep, restpos 를 다시 돌린 뒤에 넣는다. 다시 돌리지 못하면 3d2ada3 을 그대로 배포한다.
2. **측면 아랫선.** base.css:895 .pslot[data-side] 에 `transform-origin:50% 100%` 를 넣는다. 주입 실측에서 측면 bottom 이 621.3→643.6 으로 바뀌어 하단바 top 과 같아진다.
3. 844x390, 360x640 에서 CTA 가 72vh 아래로 내려가 있다. 스크롤 신호는 잘린 행뿐이다. 우선도 낮음.
4. P3: 모서리 1px 이 (198,196,192) 로 옅어진다.
5. r6/shots 의 진단 캡처 6장 정리. 점수와 무관.
6. 이월 5건 유지.

## §6 슬롭 16뿌리와 잔상 7축
슬롭 신규 0. 경계 1건 이월: 띠와 행사 카드가 같은 할인 문장을 한 화면에 2회 싣는다. 잔상 #6 한지 색과 #1 오방색 일부(朱 점)가 그대로다.

## §7 토큰 위배
- 신규: 941행 width:min(440px,100%) 이 들어오면서 440px 리터럴이 892행과 합쳐 두 곳이 됐다. --pop-w 로 묶는다.
- 이월: 544 dashed, 366~367 1100px 대 375 37.5em, 497/506/583 gap:4px, 890 rgba(.42), .ft-legal 인라인 사본 77면.

critic 실측 스크립트: /private/tmp/claude-501/-Users-gregory/5c6b0109-9488-4ecf-a4c6-00b1f0381299/scratchpad/r6c/slitfix.mjs, hop.mjs, origin.mjs (세션이 끝나면 사라질 수 있음)

## Codex X1 (메인 세션 병합)
- 판정 사유: Codex exec exited with code 0 (model gpt-6-astra, session 01a0ccdf-90f3-7ed2-a58e-b35bfb894be1). The result file is 4704 bytes and its last line is "RELEASE: NO". Codex says all 3 round-5 findings and the new focus model are resolved. It blocks the release on one P2 regression: the junction double line and 1px slit where the dim shows through between the card and the bar.
- P2 (blocks release) 접합 이중선·슬릿, 같은 결함으로 집계: base.css:900 의 투명 아래 테두리, base.css:893 의 합성 슬롯, 별도 하단바 조합에서 딤 1px 노출. 이전 0/18 → 현재 5/18, N=1도 영향. 분리 반올림 모델과 실측이 일치하지만 브라우저 내부 원인은 미확정. 최소 수리안: 모든 카드의 아래 테두리 폭을 동일하게 0으로 유지하고 측면 선은 크기에 영향 없는 inset으로 복원하는 안을 우선 검증. 활성 카드만 폭을 없애면 높이 변동이 재발할 수 있으니 접합 단일선과 → 위치 0px를 함께 재검증.
- P3 높이 차이의 빈 공간: base.css:891 에서 가장 높은 슬롯이 행 높이를 결정. 모바일 짧은 공지 위 332.5px 공백 확인, 조작 손실은 없음.
- P3 짧은 화면 CTA 스크롤 단서 부족: 잘림 자체는 문제 없음(base.css:905 높이 제한·내부 스크롤, 10/10 통과, 하단 최소 여백 25.6px). CTA를 보려면 카드 안을 스크롤해야 한다는 단서가 없음.
- 문제 없음 초점 순서: app.js:388 동적 목록 + app.js:494 inert 로 활성 카드 링크→체크→탐색→닫기 순환 유지. 모바일은 닫기가 시각적으로 윗줄인데 Tab 은 마지막(base.css:958). 기존 불일치이며 이번 회귀는 아님.
- 문제 없음 측면 카드–하단바 겹침: base.css:895 축소 + 둘째 행 분리로 겹침 0, 간격 7.7~23px.
- 문제 없음, 실제 낭독 미확인 스크린리더 안내: app.js:455 의 dialog, aria-modal, '안내 N건' 확인. 활성 내용은 탐색 가능하나 첫 초점에서 제목·본문까지 읽힌다는 보장은 없음. aria-describedby 부재만으로 결함은 아님(APG 지침은 구조화된 내용에서 생략 권장).
- 문제 없음, 창 전환 실측 미제공 초점 복귀: app.js:409 는 창 복귀 이벤트가 아닌 document focusin 처리. 외부 초점은 살아 있는 root 로 복귀하고, 수동 넘김은 새 제목으로 감(app.js:514).

## 반증 렌즈 (메인 세션 병합)
- [레이아웃 회귀 (3d2] P2: 짧은 높이 화면(모바일 배치, 폭 600 이하, 높이 약 382 미만)에서는 짧은 카드가 활성이어도 하단바 둘째 줄(← n/N → 정지)이 뷰포트 아래로 잘린다. 1행 높이를 opacity 0 인 측면 카드가 정하기 때문이다
- [레이아웃 회귀 (3d2] P2: 아래 맞춤의 빈 공간이 실제 길이의 공지에서 critic 허용치의 약 2배가 된다: 긴 공지와 짧은 공지가 함께 서면 짧은 카드가 화면 아래쪽 3분의 1에 앉는다
- [레이아웃 회귀 (3d2] P2: 카드가 3장 이상이면 → 한 번에 감아 도는 카드가 나가는 카드 위로 무대를 가로지른다. N=4 의 1920 에서는 세 번째 측면 카드가 한쪽에만 230px 드러난다
- [동작과 접근성: 첫 초] P2: 두 장 이상일 때 첫 초점이 대화상자 틀이 된 뒤로, 첫 Tab 전에는 ArrowDown, PageDown, Space 로 넘치는 활성 카드를 스크롤할 수 없음
- [동작과 접근성: 첫 초] P2: 폭 600 미만이고 높이가 약 330px 이하이면 하단바 둘째 줄(← n/N → 정지)이 화면 밖이라 포인터로 누를 수 없음
