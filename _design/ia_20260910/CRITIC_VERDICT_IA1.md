# CRITIC VERDICT — IA1 (스튜디오 단위 카드 8장 + 전형별 안내 8면 연결)

판정자 design-critic · 2026-09-10 · Blind(디렉터·구현자 점수 미열람) · 대상 = 워크트리 `~/Workspace/_wt/hh-ia` 브랜치 `ia/20260910-units-links` 구현본

---

## 0. 용도 식별 (G1, 채점 선결)

`usecase_id` = **화면(px 좌표계) 디지털 UI / v8 JTBD 01 전환(studio.html 구매 면) + 04 정보(programs/studio.html 소개면, interview/<code>.html 안내 8면) 혼합**. 반응형 2 뷰포트(1280 데스크톱, 390 모바일). 인쇄 좌표계(mm, CMYK, 블리드) 미적용, 시간축 없음.
지배차원 판정(v8 §2): 전환 면은 색(강조 1축 CTA), 정보 면은 그리드. 본 산출물은 두 면이 같은 컴포넌트를 공유하므로 **전환 면 쪽 강조축 요건이 더 엄격한 기준**이 된다.
G4 분기점 대조: 무채 저채도 팔레트, 그림자 0, 괘선 1px 단일은 이 용도에서 결함이 아니라 규범이다. 감점 근거로 쓰지 않았다.

## 1. Stage 1 자동 가드

| 가드 | 결과 | 근거 |
|---|---|---|
| G1 렌더(공백률) | PASS | 1280/390 fullPage 18장 전부 콘텐츠 렌더, 빈 캔버스 0 |
| G2 납품 가능성 | PASS | placeholder / lorem / TODO / 임시 0건. 카드 문안 전부 `_tools/r2_copy.json` + `exam_pages/facts/*.json` 원장 유래 |
| G3 safe margin | N/A | 인쇄물 아님 |
| G4 폰트 임베딩 | N/A | PDF 아님. 웹폰트 = Pretendard Variable / JetBrains Mono / Noto Serif KR |
| G5 파일 무결성 | PASS | PNG 18장 디코드 성공(PIL), 손상 0 |
| G6 종횡비 / overflow (v4 §5.1) | PASS | `results.json` 18 경로 전건 `docOverflow:0`, `pass:true`. 거대 한글 헤드(>100px) 없음 = 최대 `--t-display` clamp 상한 62px, 받침충돌 게이트 비해당 |

**Stage 1 = PASS** → Stage 2 진입.

## 2. Stage 2 독립 채점 (45 만점)

| 렌즈 | 점수 | 근거 |
|---|---|---|
| 1 내러티브-비주얼 일치 ★★★★★ | **4**/5 | 건우 지시 3항 중 ①연결·③줄바꿈은 실측으로 닫혔다. 유입 링크 실측(자기 자신·xsib 제외) = 판매 5면 각 5경로(lectures.html, studio.html, interview.html, programs/studio.html, lectures/<code>.html), 예정 3면 각 3경로. 카드 제목 `titleLines:1` 8/8. ②직관성은 부분 미달 = 카드 3액션에 primary 신호가 없고 「응시실」 라벨과 목적지가 어긋난다(아래 H2·H3) |
| 2 클리셰 ★★★★☆ | **4**/5 | 그라디언트·드롭섀도·둥근 카드·이모지 아이콘 0건(`git diff assets/base.css` 에 gradient/box-shadow 매치 0). 한자 토큰 팔레트(玄墨 #312E2E, 楮紙 #F4EFE3)와 mono 序 번호 01~08 은 국내 학원 웹 표준형이 아니다. 감점 = foot 우측 정렬 3버튼 밴드가 SaaS 가격표 카드의 기본형 |
| 3 타이포 위계 ★★★★★ | **3**/5 | h3 `--t-h4` 27.99px 8장 전부 1줄, `word-break:keep-all`, 제원 mono 13px tabular. 감점 3건: (가) ⑤와 ⑥이 모두 `btn ghost sm` 동일 클래스라 브리프 §3-2 가 지정한 primary 가 시각적으로 존재하지 않는다(`_tools/studio_units.py:56-58`). 같은 시스템의 8면은 「응시실 열기」를 solid btn 으로 쓰므로 primary 정의가 두 개다. (나) 구매 면 序 번호가 01·02·03·**05**·06 으로 04 결번(`studio.html:150-154`). (다) h3 단독으로 「국제형」·「미래캠퍼스」는 대학명을 포함하지 않는다(`.uni` 는 별도 p) |
| 4 그리드·공간 ★★★★☆ | **3**/5 | 소개면 2열 4행 고아 0, 카드 높이 8장 전부 279.4375px 동일, 좌우 패딩 s4/0 교차로 페이지 마진 정렬(실측: 좌 카드 콘텐츠 우단 613px, 괘선 640px, 우 카드 번호 좌단 668px = gutter 55px, 충돌 0). 감점 3건: (가) `.r3-unit-commerce` 빈 div 가 소개면 8장 전부에 출력되고 `grid-row:3` 이 `1fr` 이라 「지문 30편」과 foot 괘선 사이에 죽은 띠가 생긴다. (나) foot `justify-content:flex-end` 라 예정 카드 3장은 폭 500px 중 약 330px 이 빈 밴드인데 `border-top` 으로 구획되어 빈 표 행처럼 읽힌다. (다) 구매 면 5장 2열 3행 = 마지막 행 우측 고아 1칸, 소개면에서 없앤 고아가 구매 면에서 재발 |
| 5 컬러 시스템 ★★★★☆ | **3**/5 | 팔레트 5색(ink·paper·card·mat·gray) + seal 은 가격 한정, 강조 면적 10% 훨씬 아래. 예정 3장 배경 `--mat` 는 `--card` 대비 1.25:1 로 색만으로는 판별 불가하지만 배지 「9월 14일 오픈」 + 안내문 「이 단위는 9월 14일에 엽니다」 병기 = **색 단독 신호 아님, WCAG 1.4.1 통과**. 감점 2건: 같은 「판매 중」 상태가 카드에서는 무채 아웃라인, 8면 머리에서는 적계 배지로 두 표현. 그리고 전환 용도인 구매 면에 강조색 CTA 가 0개 = v8 L1 강조축이 비어 있다 |
| 6 페이지 흐름 ★★★☆☆ | **3**/5 | 소개면 배열이 판매 01·02·03 / 예정 04 / 판매 05·06 / 예정 07·08 로 지그재그라 「지금 살 수 있는 것」 스캔이 두 번 끊긴다(대학 그룹 우선 정렬의 대가). 같은 예정 3단위가 소개면에서는 카드 3장, 구매 면에서는 각주 한 줄로 위계가 세 단 차이. 8면은 착지부터 판매 CTA 까지 문서 높이 8475px 중 약 6250px(74%) 를 내려가야 한다 |
| 7 디테일 품질 ★★★☆☆ | **3**/5 | 가운뎃점·em대시 0건(`grep -o "·\|—" programs/studio.html` = 0), 제원 값 전부 `facts/<code>.json` 유래에 정수 분 검증 assert(`studio_units.py:29-31`), 8면 h1 3줄이 의미 단위로 끊긴다. 감점: 구매 면 5장이 「해설 강의 상태는 내 강의에서 확인」 동일 문장을 5회 반복, 8면 리드와 그 아래 회색 요약이 같은 21분/7분을 중복 진술, `my.html` 은 카드용 문안 「출제 유형과 풀이법 보기」를 문장 안에 넣어 §5 허브 문안 「전형별 출제 유형과 풀이법」과 어긋난다 |
| 8 매체 적합성 ★★★★☆ | **4**/5 | 반응형 경계 단일(760px), 1280 2열 / 390 1열, 18 경로 전건 `docOverflow:0`. 390 에서 제원이 3칸 가로 격자로 재구성되고 칸당 약 106px 확보 = 비좁지 않다. 고정 px 높이 0, 신규 토큰 0, `:where(body.v2)` 스코프 준수. 감점: 「담기」 tlink 는 `min-height:var(--tap)` 만 있고 min-width 가 없어 실폭 34px |
| 9 접근성·윤리 ★★★★☆ | **4**/5 | 대비 실측(sRGB→상대휘도 계산): gray/card 5.68, gray/mat 4.56, body/card 9.18, body/mat 7.37, ink/card 13.23, link/card 7.92 = 전 텍스트 AA 통과, 최저 4.56. 상태를 색 단독으로 전하지 않음. 과장 카피 0건(「최고」·「1등」·「합격 보장」 검색 0), 가격·기간은 원장 유래. 감점: heading 목록 탐색 시 h3 가 대학명을 포함하지 않고, 「담기」·「응시실」·「출제 유형과 풀이법 보기」 링크 텍스트가 한 면에 5~8회 중복(2.4.4 는 컨텍스트로 충족, AAA 미달) |
| **총점** | **31/45** | 부록A 구간 = 31~35 합격선(약한 렌즈만 보완) |

## 3. Stage 3 외부 시선

**미실시.** Stage 3 은 `design-audience-proxy` 서브 에이전트 소관이며 critic 은 디스패치 권한이 없다(expertise_v4 P0). 본 회차 발주 판정 규칙은 렌즈1·8·9 최소선과 총점만 지정했으므로 최종 판정에 영향 없음. 학부모·경쟁사·비한국 3 페르소나 평가가 필요하면 메인 세션이 별도 호출한다.

## 4. 통합 판정

- 렌즈1 = 4 (≥4 **OK**, P0 통과)
- 렌즈8 = 4 (≥3 OK) · 렌즈9 = 4 (≥3 OK)
- 총점 31 ≥ 31
- **릴리스 = YES (합격선 하단)**. 구간 하단 1점이라 아래 high 3건은 다음 회차 필수 보완으로 남긴다. 회귀 Phase 없음.

## 5. 결함 목록

### High (릴리스는 막지 않으나 다음 회차 필수)

**H1. 구매 면 序 번호 04 결번**
자리 = `studio.html:150-154`(빌드 원천 `_tools/studio_units.py` `units()` 가 CODES 8행에 1..8 을 매기고 `sync_purchase()` 가 on_sale 만 거른다). 화면은 01·02·03·05·06 으로 읽힌다.
수리 = 구매 면 렌더 시 번호를 다시 매기거나(01~05), 序 번호를 순서 마커가 아닌 단위 식별자로 읽히게 라벨 앞에 고정 접두(예: 원장 코드 표기)를 붙인다. 둘 중 하나를 골라 두 면의 규칙을 명시한다.

**H2. 카드 3액션에 primary 가 없다**
자리 = `_tools/studio_units.py:56-58` (⑤와 ⑥이 동일 `btn ghost sm`), 렌더 결과는 `programs/studio.html:113-120` · `studio.html:150-154`.
수리 = 소개면은 ⑤ 「출제 유형과 풀이법 보기」를 solid `btn`(8면의 「응시실 열기」와 같은 등급)으로 올리고 ⑥은 ghost 유지. 구매 면은 ⑦ 「담기」를 solid `btn` 으로 올린다(면의 목적에 따라 primary 를 갈라야 한다).

**H3. 구매 면의 목적 행동이 가장 약한 처리다**
자리 = `studio.html:150-154` 의 `<button class="tlink" data-r3-unit-cart>담기</button>`. 전환 용도 면에서 강조축이 비어 있고(v8 L1), 사용자 눈에는 ghost 버튼 2개가 먼저 들어온다.
수리 = H2 와 함께 집행. 구매 면 카드 foot 의 순서를 ⑦ ⑤ ⑥ 로 바꾸거나 ⑦만 solid 로 승급한다.

### Medium

**M1. 「응시실」 라벨과 목적지 불일치**
자리 = 소개면 `programs/studio.html` 카드 = `../studio.html?unit=<code>`(= 구매 면), 구매 면 `studio.html:508` = `preventDefault` 후 `selectUnit()` 로 같은 면 탭 전환. 두 경우 모두 응시실이 열리지 않는다.
수리 = 미구매자에게는 「세트 고르기」 또는 「이용권 보기」로 문면을 바꾸고, 보유자에게만 「응시실」을 남긴다. 문안 변경이므로 §5 승인 문안표 갱신 결재가 선행한다.

**M2. 소개면 카드에 빈 슬롯이 출력된다**
자리 = `_tools/studio_units.py:71` 의 `'<div class="r3-unit-commerce"></div>'` 가 prefix/purchase 무관하게 항상 출력 + `assets/base.css` `.r3-unit{grid-template-rows:auto auto 1fr auto}`.
수리 = 소개면 렌더에서는 해당 div 를 출력하지 않고, 행 사양을 `auto auto auto auto` 로 두되 높이 균일은 `align-items:stretch` + foot `align-self:end` 로 유지한다.

**M3. 구매 면 마지막 행 고아 1칸**
자리 = `studio.html` `.units r3-units` 2열 × 판매 5장.
수리 = 예정 3단위를 각주 한 줄이 아니라 6번째 칸의 비구매 카드(안내 링크만)로 올리면 고아가 사라지고 소개면과 위계도 맞는다. 또는 5장일 때만 마지막 칸에 「9월 14일에 여는 단위」 블록을 넣는다.

**M4. 예정 카드 foot 밴드가 비어 보인다**
자리 = `assets/base.css` `.r3-unit .foot{justify-content:flex-end}`, 렌더 결과 = 소개면 04·07·08.
수리 = 액션이 1개일 때는 `justify-content:flex-start` 로 전환하거나, 안내문 「이 단위는 9월 14일에 엽니다」를 foot 좌측으로 옮겨 행을 채운다.

### Low

**L1. 「담기」 tap target 폭 34px**
자리 = `assets/base.css` `.r3-unit .foot :is(.btn,.tlink){min-height:var(--tap)}` 에 min-width 없음. WCAG 2.2 SC 2.5.8 AA(24×24)는 통과, 44 권장선 미달.
수리 = `min-width:var(--tap)` 추가 또는 좌우 패딩 `--s2` 부여.

**L2. `my.html` 문안이 §5 자리 배정과 어긋난다**
자리 = `my.html` 의 `'해설 강의 <a class="tlink">출제 유형과 풀이법 보기</a>'`.
수리 = 허브 성격 자리이므로 「전형별 출제 유형과 풀이법」로 바꾸거나, §5 표에 `my.html` 행을 추가해 현행을 승인한다.

## 6. 자체 검사

### AI 슬롭 16뿌리
텍스트 떡칠 0(카드당 텍스트 요소 7, 칸 폭 500px) · 가짜 Bold 0 · 보라 청록 그라디언트 0(diff 매치 0) · 가짜 3D / 드롭섀도 0 · 최상급 표현 0 · AI 생성 이미지 0(이미지 자체 0) · Inter / Space Grotesk 0(Pretendard + JetBrains Mono + Noto Serif KR) · 이모지 아이콘 0 · 중앙정렬 카드 3연 0(좌정렬 명패 행).
**약한 해당 1건** = 낚시 비매칭 계열. 「응시실」이 응시실로 가지 않는다(M1).

### 잔상 7축
1. 구 5장 하드코딩 → 제거, 빌더 생성으로 대체 (해소)
2. 구 `auto-fit minmax(11rem)` 5열 → 제거 (해소)
3. 외톨이 `<a href="#buy">담기</a>` → 카드 ⑦로 흡수, 잔존 `#buy` 앵커 1건은 별 섹션의 「구매할 상품 고르기」 solid btn 으로 정당 (해소)
4. `r3_yonsei_spec` / `r3_korea_spec` 미사용 키 → 소비자 0 확인 (해소)
5. `.r3-unit-commerce` 빈 슬롯이 소개면에 잔존 (**잔상 1건**, M2)
6. 8장 체계의 序 번호가 5장 구매 면에 그대로 남음 (**잔상 1건**, H1)
7. `.r3-units` 구 2열 미디어쿼리 → 제거 (해소)

### 채점 자체 반증
초기 판독에서 「좌 카드 제원값과 우 카드 번호가 3px 로 충돌」·「담기가 카드 밖으로 잘림」 2건을 결함으로 잡았으나, 픽셀 재측정에서 둘 다 기각했다. 원인 = 밝기 임계 140 이 괘선 색(휘도 139.3)을 글리프로 계상. 실제 gutter 55px, 「담기」 우단 615px 대 콘텐츠 상자 616px. expertise_v4 §4 「critic 오분석 자각, 검증 먼저」 적용 결과이며 두 건은 결함 목록에서 뺐다.


---

# Δ 재채점 (수리 후, 2026-09-10 15:23 캡처 · 재빌드 fe2615c748ca89cf)

Blind 유지(구현자 자체 점수 미열람). 수리 주장은 전부 원천과 새 캡처로 재측정한 뒤에만 Δ 를 인정했다.

## Δ-1 수리 검증 (주장 대 실측)

| 항목 | 주장 | 실측 | 판정 |
|---|---|---|---|
| H1 결번 | 구매면 01~05 재계수 | `studio.html` `kn` = 01·02·03·04·05, `programs/studio.html` = 01~08. 원천 = `_tools/studio_units.py:78-81` `sync_purchase()` 가 on_sale 만 `enumerate(...,1)` 로 다시 센다 | 해소 |
| H2 primary | 소개면 ⑤ solid | `studio_units.py:55` `guide_cls = "btn ghost sm" if purchase else "btn sm"`. 캡처에서 소개면 8장 전부 ⑤ 가 solid 먹판, ⑥ ghost, ⑦ tlink | 해소 |
| H3 구매 행동 | 구매면 ⑦ solid | `studio_units.py:62` `<button class="btn sm" data-r3-unit-cart>`, 캡처 5장 전부 「담기」 solid, ⑤⑥ ghost. 면당 solid 1개 규칙 성립 | 해소 |
| M2 빈 슬롯 | 소개면 미출력 | `studio_units.py:71` 이 `purchase` 일 때만 출력. DOM 은 깨끗해졌다. **다만 기하는 무변** = 옛 캡처와 새 캡처의 카드 내부 밴드가 완전히 같다(문면 328~338, foot 괘선 376, 버튼 393~440, 카드 하단 465). `grid-template-rows` 의 3행 `1fr` 이 남아 stretch 여유를 그대로 흡수한다 | 부분 해소 |
| L1 tap 폭 | tlink min-width | `assets/base.css:1420` `.r3-unit .foot .tlink{min-width:var(--tap);justify-content:center}`. 적용 대상은 소개면 「담기」 1종(구매면 담기는 button 으로 승급) | 해소 |
| 무결성 | 캡처 20/20 | `results.json` entries 20, `pass` 전건 true, `errors` 0, `docOverflow` 합 0, `titleLines` 1 전건, 카드 높이 소개면 279.4375 / 구매면 328.40625 동일 | 확인 |

## Δ-2 렌즈 점수

| 렌즈 | 전 | 후 | Δ | 변동 근거 |
|---|---|---|---|---|
| 1 내러티브 ★★★★★ | 4 | **4** | 0 | 직관성 미달 2건 중 primary 부재는 해소, 「응시실」 라벨 대 목적지 불일치(M1)와 8면 CTA 74% 깊이는 그대로. 미해결 약속 불일치가 남아 5(교범)에 못 미친다 |
| 2 클리셰 ★★★★☆ | 4 | **4** | 0 | 변경 없음. 새 solid 는 팔레트 안 먹판이라 그라디언트·섀도 유입 0 |
| 3 타이포 위계 ★★★★★ | 3 | **4** | **+1** | 감점 3건 중 2건 해소(primary 부재, 04 결번). 남은 것 = h3 단독에 대학명 부재. 신규 관찰 2건 = 같은 단위가 소개면 05 대 구매면 04 로 순번이 갈린다(면 안 순번 규칙의 대가), 그리고 `studio.html` 한 면에 「담기」가 solid `btn sm`(단위 카드 5) · `btn ghost sm`(지문 낱권) · `btn ghost`(공통 인강) 세 등급으로 공존한다 |
| 4 그리드·공간 ★★★★☆ | 3 | **3** | 0 | 감점 3건 중 M2 만 DOM 수준 해소이고 화면 기하는 픽셀 동일. M3 구매면 고아 1칸과 M4 예정 카드 foot 빈 밴드는 보류 선언. solid 승급으로 예정 카드 foot 은 먹판 1개 대 좌측 약 330px 공백이 되어 대비가 오히려 뚜렷해졌다 |
| 5 컬러 시스템 ★★★★☆ | 3 | **4** | **+1** | 감점 2건 중 「구매 면 강조축 공백」 해소. 면당 solid 1개로 v8 L1 강조축 단일성 성립(강조는 hue 가 아니라 `--ink` 먹판 값대비로 집행, seal 은 가격 전용 유지). 남은 것 = 「판매 중」이 카드 무채 아웃라인과 8면 적계 배지 두 표현 |
| 6 페이지 흐름 ★★★☆☆ | 3 | **3** | 0 | 지그재그 배열, 예정 3단위의 두 면 위계 3단 차, 8면 CTA 깊이 전부 미집행 |
| 7 디테일 품질 ★★★☆☆ | 3 | **3** | 0 | 반복 문장 5회, 8면 리드 중복 진술, `my.html` 문안(L2) 전부 미집행 |
| 8 매체 적합성 ★★★★☆ | 4 | **4** | 0 | L1 해소 확인(min-width `--tap`, `tooSmall` 0). 5(교범)에 못 미치는 이유 = 같은 「담기」 행동이 소개면 `<a href="#buy">` 와 구매면 `<button type="button">` 두 semantics 로 갈린다(각 5건 실측) |
| 9 접근성·윤리 ★★★★☆ | 4 | **4** | 0 | 대비 실측 변동 없음(팔레트 무변경, 최저 gray/mat 4.56 AA). solid 먹판 `--ink`/`--card` = 13.23:1. 남은 것 = h3 대학명 부재, 링크 텍스트 면당 중복, 담기 semantics 분기 |
| **총점** | **31** | **33/45** | **+2** | 구간 = 31~35 합격선 유지, 하단에서 중단으로 이동 |

## Δ-3 판정

- 렌즈1 = 4 (≥4 **OK**, P0 통과) · 렌즈8 = 4 (≥3 OK) · 렌즈9 = 4 (≥3 OK) · 총점 33 ≥ 31
- **릴리스 = YES (합격선)**. 회귀 Phase 없음.
- **남은 High = 0.** H1·H2·H3 전건 실측 해소.

## Δ-4 남은 항목

Medium
- **M1** 「응시실」 라벨 대 목적지 불일치 (결재 큐 IA1-3, §5 문안표 갱신 선행)
- **M2'** 빈 슬롯은 없어졌으나 죽은 여백은 그대로. 수리 = `.r3-unit{grid-template-rows:auto auto 1fr auto}` 의 3행을 소개면에서 `auto` 로 접고 높이 균일은 `align-items:stretch` + foot `align-self:end` 에 맡긴다
- **M3** 구매면 마지막 행 고아 1칸 (보류 선언)
- **M4** 예정 카드 foot 빈 밴드 (보류 선언, solid 승급으로 대비 증가)
- **M6 (신규)** `studio.html` 한 면에 「담기」 3등급 공존(solid / ghost sm / ghost). 수리 = 단위 카드만 solid 로 두고 낱권·인강 담기의 두 ghost 크기를 하나로 통일한다

Low
- **L2** `my.html` 문안 (보류 선언)
- **L3 (신규)** 같은 단위의 序 번호가 두 면에서 다르다(소개면 05 = 구매면 04). 면 안 순번 규칙은 명시됐으므로 결함이 아니라 대가로 기록한다. 두 면을 오가는 사용자가 번호로 대조하지 않도록 카드 표제가 유일 식별자임을 유지한다
- **L4 (신규)** 「담기」 semantics 가 `<a href="#buy">`(소개면 5건)와 `<button>`(구매면 5건)로 갈린다. 수리 = 담기는 양쪽 다 `<button>`, 구매 섹션으로 이동만 하는 링크는 별 문안으로 분리

## Δ-5 자체 검사 (재실행)

- AI 슬롭 16뿌리: 그라디언트·섀도·가짜 3D·최상급 표현·이모지 전건 0 유지(`git diff assets/base.css` gradient/box-shadow 매치 0). 약한 해당 1건(라벨 비매칭 M1) 존치
- 잔상 7축: 5번(빈 `.r3-unit-commerce`) 해소, 6번(8장 序 번호가 5장 면에 잔존) 해소 → **잔상 0건**
- 채점 자체 반증: 「짝수 열 solid 버튼이 카드 우단에 0px 로 붙어 잘려 보인다」를 결함 후보로 잡았다가 기각했다. 실측에서 홀수 카드 버튼 우단 615px(콘텐츠 상자 616px), 짝수 카드 1148px(콘텐츠 상자 1149px)로 양쪽 다 자기 상자에 정렬돼 있고, 짝수 열의 우단은 같은 면 가격 495,000원(1146px)·제원과 같은 페이지 마진선이다. 2열 괘선 격자에서 바깥 모서리가 마진선인 것은 규범이지 결함이 아니다


---

# Δ2 재채점 (Stage 3 지목 수리 후, 2026-09-10 15:40 캡처)

수리 = `assets/base.css:1426-1429` `@media(min-width:47.5em)` 안 `.r3-unit, .r3-unit:nth-child(even){padding-inline:var(--s4)}`. 390 은 `.unit` 규범(패딩 0) 유지라 무변.

## Δ2-1 수리 검증 (실측)

| 축 | 실측 | 판정 |
|---|---|---|
| 우측 지각 절단 | 짝수 열 「담기」 solid 우단 1111px, 내용 상자 1124.8px, 카드 색면 상자 1148.8px = 색면이 버튼보다 약 24px 더 나간다. `zoom_r3_studio_right_edge.png` 에서 먹판 오른쪽에 `--card` 면이 남고 그다음이 `--paper` 다 | **해소** |
| 좌우 대칭 | 홀수 내용 155.2~616(상자 131.2~640), 짝수 665~1124.8(상자 640~1148.8). 괘선 기준 안쪽 24px 대칭 | 확인 |
| 무결성 | `results.json` 20/20 pass, `errors` 0, `docOverflow` 합 0, 카드 높이 소개면 279.4375 / 구매면 328.40625 불변, `titleLines` 1 전건 | 확인 |
| **신규 비용 1** | 카드 내용 좌단 155.2px 가 같은 면의 섹션 제목·도입문·예정 각주가 쓰는 페이지 스파인 131.2px 와 24px 어긋난다(실측: h2 「응시 단위 고르기」 글리프 좌단 132px 대 카드 「01」 글리프 좌단 158px). 디렉터 B안 「명패 행」이 셀 패딩을 가진 표로 읽힌다 | **신규 결함** |
| **신규 비용 2** | 한 카드 안에 괘선 길이가 두 종류다. 행 구분 괘선(`.r3-unit` border-bottom)은 셀 폭 131~640 전폭, foot 상단 괘선은 내용 폭 155~616. 수리 전에는 둘이 좌단 131 을 공유했다 | **신규 결함** |

## Δ2-2 렌즈 점수

| 렌즈 | Δ1 | Δ2 | Δ | 근거 |
|---|---|---|---|---|
| 1 내러티브 ★★★★★ | 4 | **4** | 0 | 링크 구조·문안 무변. M1 존치 |
| 2 클리셰 ★★★★☆ | 4 | **4** | 0 | 무변 |
| 3 타이포 위계 ★★★★★ | 4 | **4** | 0 | 序 번호·위계·제목 줄 수 전부 불변 |
| 4 그리드·공간 ★★★★☆ | 3 | **3** | 0 | 결함 1개를 결함 1개와 맞바꿨다. 해소 = 우측 지각 절단(Stage 3 3인 동일 지목, 내 픽셀 축에는 없던 항목). 신규 = 좌측 스파인 24px 이탈. 기존 3건(M2' 죽은 여백, M3 고아, M4 foot 빈 밴드)은 그대로다. 순증감 0 |
| 5 컬러 시스템 ★★★★☆ | 4 | **4** | 0 | 팔레트·대비 무변경. 색면이 글자보다 넓어져 `--mat` 예정 카드의 상태 색면이 독립 판독되는 이득은 있으나, 남은 감점(「판매 중」 카드 무채 아웃라인 대 8면 적계 배지)이 그대로라 5 미도달 |
| 6 페이지 흐름 ★★★☆☆ | 3 | **3** | 0 | 무변 |
| 7 디테일 품질 ★★★☆☆ | 3 | **3** | 0 | 색면 끝과 글자 끝이 겹치던 마무리는 해소. 대신 한 카드 안 괘선 두 길이가 생겼다. 기존 3건(반복 문장 5회, 8면 중복 진술, my.html 문안) 존치. 순증감 0 |
| 8 매체 적합성 ★★★★☆ | 4 | **4** | 0 | 20/20 pass, overflow 0, 높이·제목 줄 수 불변, 390 무변. 담기 semantics 분기 존치 |
| 9 접근성·윤리 ★★★★☆ | 4 | **4** | 0 | 팔레트 무변경이라 대비 실측 동일(최저 gray/mat 4.56 AA, ink/card 13.23) |
| **총점** | 33 | **33/45** | **0** | 구간 31~35 합격선 유지 |

## Δ2-3 판정

- 렌즈1 = 4 (≥4 **OK**) · 렌즈8 = 4 (≥3 OK) · 렌즈9 = 4 (≥3 OK) · 총점 33 ≥ 31
- **릴리스 = YES (합격선)**. 회귀 Phase 없음. **남은 High = 0.**
- 총점이 안 움직인 것은 수리가 무익했다는 뜻이 아니다. 해소된 항목은 Stage 3 지각 축에 있었고 내 45점 렌즈 원장에는 결함으로 올라 있지 않았다. 반대로 신규 비용 2건은 렌즈4·7 원장에 새로 올랐다. **Stage 3 재평가는 audience-proxy 소관이며 그쪽 점수는 오를 여지가 있다. critic 총점 불변과 별개로 판단한다**(critic 은 디스패치 권한 없음)

## Δ2-4 권고 (다음 회차, High 아님)

**Δ2-R1 스파인 보존형 대안.** 지각 절단은 오른쪽 열에서만 지목됐고 실측도 그렇다. 홀수 카드는 수리 전에도 내용 우단 616px 대 괘선 640px 로 이미 24px 을 갖고 있었다. 따라서 양쪽 열에 `padding-inline` 을 주는 대신 **짝수 열의 우측만** 여는 안이 스파인을 지킨다.

```
@media(min-width:47.5em){
  :where(body.v2) .r3-unit{padding:var(--s4) var(--s4) var(--s4) 0}
  :where(body.v2) .r3-unit:nth-child(even){padding:var(--s4);border-left:var(--rule-ui)}
}
```
결과 = 홀수 내용 131.2~616(페이지 스파인 유지), 짝수 내용 664~1124.8(우측 24px 확보). 잃는 것 = 짝수 열 우단이 페이지 우측 마진선 1148.8px 과 어긋난다. 좌측 스파인은 h2·도입문·각주 3자리가 공유하는 가시선이고 우측 마진선은 카드 밖에 공유 요소가 없으므로, 두 손실 중 작은 쪽이다.

**Δ2-R2 괘선 길이 통일.** foot 상단 괘선을 셀 폭으로 되돌리려면 `.r3-unit .foot` 에 `margin-inline:calc(var(--s4) * -1)` + `padding-inline:var(--s4)` 를 주어 괘선만 131~640 을 긋고 내용은 안쪽에 둔다. 또는 행 구분 괘선을 내용 폭으로 줄여 둘을 맞춘다. 어느 쪽이든 한 카드 안 괘선은 한 길이여야 한다.

## Δ2-5 자체 검사 (재실행)

- AI 슬롭 16뿌리: 무변, 그라디언트·섀도·가짜 3D·최상급 표현·이모지 0. 약한 해당 1건(M1 라벨 비매칭) 존치
- 잔상 7축: 0건 유지
- 채점 자체 반증: Δ1 에서 내가 「짝수 열 solid 버튼이 자기 상자에 정렬돼 있으므로 결함 아님」으로 기각한 판정을 Stage 3 3인 동일 지목이 뒤집었다. 픽셀 정렬(overflow 0)과 지각 판독(절단으로 읽힘)은 다른 축이며, 색면 경계와 글자 경계가 같은 선에 놓이면 정렬이 맞아도 잘린 것으로 읽힌다. **본 건을 채점 앵커로 기록한다: 색면이 있는 셀에서는 내용 경계가 색면 경계보다 안쪽이어야 하고, 이는 `docOverflow` 로는 검출되지 않는다.**
