# CRITIC_VERDICT_2, /interview/ 8면 수리본 재채점 (design-critic 독립 2차, 2026-09-10)

대상 = `~/Workspace/hyunhak-site/interview/*.html` 8본 + `assets/exam_page.css` (수리 후 mtime 11:48).
근거 = 본 세션이 `http://127.0.0.1:8911` 라이브 렌더에 직접 돌린 Playwright `getBoundingClientRect` 8면 x 1280/390 = 16 조합 + 구획 클립 렌더 육안. 메인 세션 실측값은 재사용하지 않았고 자기신고는 근거로 세지 않았다. 파일 수정 0.

## 0. Stage 1 자동 가드

G1 렌더 = 16/16 조합 페이지 오류 0. G6 종횡비와 overflow = `scrollWidth - clientWidth` 0 (16/16), 표 셀 우측 뷰포트 초과 0, 절대배치 이탈 0, 구획 내 형제 세로 겹침 0. G2 납품과 G3 여백과 G4 폰트와 G5 무결성은 인쇄 납품물 대상이라 본 건 비해당. Stage 2 진입 허용.
Stage 3 외부 시선 3 페르소나는 발주 범위 밖이라 미실시. 아래 릴리스 판정은 Stage 1 과 Stage 2 근거다.

## 1. 수리 2건 실측 검증

**B-1 닫힘.** `assets/exam_page.css` 129~140행에서 6행 전부 `:where(body.xa) .xtime .cap,:where(body.xc) .xtime .cap{...}` 꼴로 두 선택자를 완전하게 적었다. `_design/exam_page.css` 정본과의 diff 는 이 6행과 주석 1행, 그리고 빌더용 추가 3규칙(`.xsrc`, `.xtype .n>span:not(.k)`, `.xtime .row .xsec.txt`)뿐이며 추가분도 전부 `var()` 토큰이다.
렌더 실측 8/8 면 x 2 폭: `.xtime` computed position = `static`, 폭 1009.6px(1280) 및 342px(390), `.xtime .row` 폭 1008.6px 및 341px, 폭 0 행 0개. 세로 타임라인이 좌측 굵은 괘선과 원형 노드와 막대로 의도대로 선다.

**B-2 닫힘.** 빌더 산출이 `<li><span><b>제목</b> 본문</span></li>` 이고 `.xlock ul li` 도 `<li><span>...</span></li>` 자식 1개다. 실측 `.xrules li` 높이 73px(1280 전면 동일), 101~129px(390), `.xlock ul li` 19.5px, 종전 1,069~1,293px 에서 회복. 세로 깔림 검출기(폭 180px 미만이면서 높이 260px 초과) 적발 0 (16/16 조합).

**W-1 닫힘.** 각주 참조 집합과 정의 집합이 8/8 면에서 일치한다. korea-hum {1,2,3}={1,2,3}, korea-eq-sci {1,2,3,4}={1,2,3,4}, yonsei-mirae {1,2,3,4}={1,2,3,4}. 고아 0.
**W-2 닫힘.** 오픈 예정 3면(korea-eq-hum, korea-eq-sci, yonsei-mirae)의 `#plan` 안 「공지 보기」 링크는 `.xsoon` 띠 안 1개뿐이다(`../notice.html?id=ntc_0914a001`). 띠 밖 중복 링크 0.
**W-3 미반영 확인.** 아래 렌즈 4 근거.
**W-4 미반영 확인.** `.k` computed letter-spacing 1.2px, word-spacing 0px, font-size 12px 그대로.

## 2. 9렌즈 독립 채점

| 렌즈 | 점수 | 실측 근거 |
|---|---|---|
| 1 내러티브-비주얼 일치 | **4**/5 | 면의 약속 두 축이 복귀했다. 출제 유형 지도 5유형이 발문과 빈도와 열쇠 4열로 서고, 풀이 메타는 원칙 5항(각 73px)과 1,260초 5단계 세로 타임라인(막대 폭이 단계별로 갈림)으로 선다. 전환 축은 판매 면 solid 1개(「면접 스튜디오 이용권 보기」)와 텍스트 링크 1개, 오픈 예정 면은 玄墨 띠 1개로 단일하다. 감점 1 = 약속의 핵심인 「읽는 열쇠」가 `.xtype` 4열 중 가장 좁은 128px 열에 들어가고, 덜 중요한 정의와 발문이 360px 과 313px 을 가져간다. 비중 배분이 약속과 역순 |
| 2 클리셰 | **4**/5 | 라이브 CSS grep: gradient 0, Inter 0, Space Grotesk 0, `url(data:image` 0, `nth-child(even)` 0, rgba 발광 그림자 0, `:root` 밖 색 리터럴 0. HTML 안 `linear/radial-gradient` 0, `<img>` 1개(연구소 로고). 합격 배지와 후기 카드 0. 4단 준비 순서와 4열 유형 지도는 관습적 골격 |
| 3 타이포 위계 | **4**/5 | 산문 measure 576px, 본문 16px/24px, 계기값 JetBrains Mono tabular-nums(「1,260초 5단계」, 「30세트」, 「44편」), 규격표 값과 라벨 분리. 1글자 행폭 붕괴는 소멸. 감점 1 = `.xtype` 4열이 128px 라 37자가 3행, 27자가 2행으로 행당 12~13자에 그친다(한국어 본문 권장 하한 미달), 5/5 행 전부. 그리고 `.k` 자간 1.2px 가 어절을 벌려 「기출과  은행」, 「이  면에  싣지  않는  층」으로 읽힌다 |
| 4 그리드 공간 | **3**/5 | 판면 1018px, 절대배치 이탈 0, 겹침 0. 감점 2 = `.xtype` 4열 행에서 열별 높이 편차가 130.4~169.4px, 5/5 행 전부 발생한다(방법론 적용 행 190.5px 대 21.1px). grid-template-columns 실측 144px / 360.3px / 313.3px / 128px 로, 내용량 75자인 열이 128px, 31자인 열이 360px 를 차지해 폭 배분이 내용량과 역비례한다. 1열 제목 아래가 매 행 수동적 여백으로 남는다 |
| 5 컬러 시스템 | **5**/5 | 수리 diff 로 색 토큰 무변경 확인, `:root` 밖 색 리터럴 0, 추가 3규칙도 `var(--gray)`와 `var(--body)`와 `var(--sans)` 사용. 朱印은 번호 카운터와 각주 기호와 활성 앵커 밑줄에만, 金泥는 `.xsoon` 띠 위 날짜 1자리에만, 반전 표면 1개 |
| 6 페이지 흐름 | **4**/5 | `#method` 1,160~1,305px(종전 6,664~8,480px). 1280 구획 높이 285~1,305px, 최대 대 최소 4.6배로 리듬이 산다. 문서 높이 1280 = 7,808~8,375px 로 디렉터 시안 A 6,567px 대비 1.19~1.27배, 내가 1차에 권고한 1.5배 정지선 이내. 오픈 예정 면 CTA 중복 소멸. 감점 1 = 상반부 spec 1,170 / types 1,225 / method 1,305 세 구획이 연속 근접해 스크롤 초반 리듬이 평탄하다 |
| 7 디테일 품질 | **4**/5 | 각주 고아 0(8/8), 「대학 기출 지문이 아닙니다」 고지, `tel:` 링크, `aria-current` 활성 앵curr 표시 렌더 확인, FAQPage JSON-LD, 콘솔 오류 0(16/16). 감점 1 = `.k` 자간 어절 분리가 전 면에 남고, 시간 배분 5행 중 3행이 「약 420초」로 값이 같아 막대 길이만 정보를 지고 숫자 열은 반복된다 |
| 8 매체 적합성 | **4**/5 | 390 가로 넘침 0(8/8), 표 셀 초과 0, 히트박스 44px 미만 0. sticky 2단(헤더 65px + 앵커 내비 49px = 114px)과 `scroll-margin-top` 105px/112px 정합 실측: 앵커 점프 후 구획 상단이 sticky 하단보다 54.6px(390) 및 56.0px(1280) 아래에 놓여 가림 0. 하단 고정 탭바 65px 대 body padding-bottom 72px 로 말미 콘텐츠 도달 가능. `.xtime .row` 390에서 1열 전환 확인. 감점 1 = 390 문서 높이 11,020~11,844px 로 1280의 1.4배, 모바일 스크롤 총량이 길다 |
| 9 접근성 윤리 | **4**/5 | 1차 감점 사유였던 시각 순서 대 DOM 순서 불일치(WCAG 1.3.2)가 `.xtime` static 복귀로 해소. 색 토큰 무변경이라 1차 대비 실측(본문 8.13:1, 규격표 값 11.73:1, `.k`와 `.note` 5.04:1, `.xsoon` 본문 7.78:1, 金泥 날짜 6.28:1) 전건 AA 유지. 과장 카피 0, 출처 각주 명시, 비공개 층은 회색 자리표 없이 정직 고지. 감점 1 = 12px `.k` 가 5.04:1 로 AA 는 넘되 AAA 미달이고 자간 분리로 판독 부담이 겹친다 |
| **총점** | **36**/45 | |

## 3. 릴리스 판정

**릴리스 YES.**

- P0 오버라이드: 렌즈1 = 4, 임계 4 이상 충족.
- P1 보조: 렌즈8 = 4, 렌즈9 = 4, 임계 3 이상 충족.
- 부록A 구간: 36~40 = 출판 가능.
- 차단 결함 0건. 1차 차단 2건 모두 실측으로 닫혔다.

## 4. 잔여 권고 3건 (차단 아님, 승계 세션 몫)

**R-1. `.xtype` 4열 폭 배분과 행 높이 편차.** 위치 = `assets/exam_page.css` 의 `.xtype` grid 규칙, 실측 144 / 360.3 / 313.3 / 128px.
왜 = 내용량 75자 열이 128px(행당 12~13자), 31자 열이 360px 다. 결과로 매 행 130.4~169.4px 의 수동적 여백이 1열 아래 남고, 면의 핵심인 「읽는 열쇠」가 가장 좁게 조판된다. 5/5 행, 8/8 면.
방향 = 4열을 2행 2열로 접거나(제목과 정의 위, 발문과 열쇠 아래), 열 폭을 내용량에 맞춰 144 / 300 / 260 / 240px 대로 재배분한다. 열쇠 열 measure 를 최소 20자로 올린다.

**R-2. `.k` 라벨 자간.** letter-spacing 1.2px, word-spacing 0px, 12px. 어절이 벌어져 「기출과  은행」으로 읽힌다. 자간을 0.8px 로 낮추거나 `word-spacing:-0.2em` 보정을 준다.

**R-3. 시간 배분 값 반복.** `.xtime` 5행 중 3행이 「약 420초」로 동일하다. 막대 폭은 갈리므로 숫자 열을 「420초」 대신 비율이나 누계로 바꾸면 열이 정보를 지게 된다.

## 5. 1차 대비 델타

| 렌즈 | 1차 | 2차 | Δ |
|---|---|---|---|
| 1 | 1 | 4 | +3 |
| 2 | 4 | 4 | 0 |
| 3 | 2 | 4 | +2 |
| 4 | 2 | 3 | +1 |
| 5 | 5 | 5 | 0 |
| 6 | 2 | 4 | +2 |
| 7 | 2 | 4 | +2 |
| 8 | 2 | 4 | +2 |
| 9 | 3 | 4 | +1 |
| 총점 | 23 | 36 | **+13** |

차단 2건 0건, 권고 4건 중 2건 닫힘(W-1, W-2), 2건 잔존(W-3 = R-1, W-4 = R-2), 신규 권고 1건(R-3).
1차에 켜지 않은 셀프 편향 플래그는 2차에도 켜지 않는다. 디렉터 자체 34 대 critic 36 으로 Δ = 2, 임계 3 미만.

**16뿌리 적발 = 0/16.** 라이브 CSS 와 HTML 재검으로 확인(gradient, 생성 이미지, Inter, Space Grotesk, 발광 그림자, 지브라 표, data URI 이미지, 색 리터럴 전부 0).
**잔상 7축 훼손 = 0축.** 1차에 훼손 1건이던 #4 한글 자소 축(괘선과 여백으로 세우는 회색도)이 `#method` 복구로 회복했다. #6 한지 질감 축은 1차와 같이 온전하다.

## 6. 승계 세션에 남기는 검출기 권고

1차 §5 의 3개 중 2개는 이번 수리로 유효성이 입증됐다. 상주시킬 값:
- 면 길이 기준선 = 시안 A 1280 6,567px 대비 1.5배(9,850px) 초과 시 정지. 현재 실측 7,808~8,375px.
- 세로 깔림 오라클 = 폭 180px 미만이면서 높이 260px 초과인 `main` 후손 수. 현재 0, 회귀 시 즉시 양수.
- 절대배치 이탈 = `main` 안 `position:absolute` 이면서 `.page` 조상이 없는 요소 수. 현재 0.
- 신규 추가 권고 = 격자 행 내 셀 높이 편차 상한. 현재 `.xtype` 이 130~169px 로 위반 중이므로, R-1 수리 후 임계 60px 로 상주시킨다.
